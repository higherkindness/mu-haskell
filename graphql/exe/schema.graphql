type Author {
    id: Int!
    name: String!
    books: [Book!]!
}

type Book {
    id: Int!
    author: Author!
    title: String!
}

type Query {
    author(name: String! = ".*"): Author
    authors: [Author!]!
    book(title: String! = ".*"): Book
    books: [Book!]!
}
